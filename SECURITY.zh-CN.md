# 安全策略

## 适用范围

这个仓库主要面向自托管部署。运行时凭证、网络暴露方式和上游代理访问控制由部署者自行负责。

## 漏洞反馈

如果发现疑似安全漏洞，不要直接公开提 issue。

请先通过维护者的私下渠道反馈，确认后再决定是否公开披露。

## 最低加固要求

- 替换默认管理员用户名和密码
- 替换示例加密密钥
- 不要把 Redis 直接暴露到公网
- 尽量放在防火墙、内网或可信入口之后
